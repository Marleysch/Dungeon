Creates very basic dungeon crawler type game

Reads in a map from file and user moves through map and can encounter monsters, healing, the finish, or nothing

Uses factories to create monster objects 